@model List<DataLayer.AboutUsItem>
<div class="card card-info m-2">
    <div class="card-header">
        <h3 class="card-title">آیتم های نرم افزار در صفحه درباره ما</h3>
    </div>
    <!-- /.card-header -->
    <!-- form start -->
    @foreach (var item in Model)
    {
        <div class="m-2">
            <p>@item.ItemTitle</p>
        </div>
    }
    <div class="card-footer">
        <a href="/Admin/AboutUsItems" class="btn btn-info d-block w-100">مشاهده ی لیست</a>
    </div>
</div>