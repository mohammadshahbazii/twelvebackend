@model DataLayer.AboutUsItem
<div class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card card-danger">
                    <div class="card-header">
                        <h3 class="card-title">
                            حذف آیتم
                            <span class="text-dark mx-2">@ViewBag.Message</span>
                        </h3>
                    </div>
                    <!-- /.card-header -->
                    <!-- form start -->
                    <form role="form" method="post" enctype="multipart/form-data">
                        <input type="hidden" name="AboutUsItemId" id="AboutUsItemId" value="@Model.AboutUsItemId" />
                        <input type="hidden" name="ImageName" id="ImageName" value="@Model.ImageName" />
                        <input type="hidden" name="ItemTitle" id="ItemTitle" value="@Model.ItemTitle" />
                        <input type="hidden" name="ItemDescription" id="ItemDescription" value="@Model.ItemDescription" />
                        <div class="card-body">
                            <div class="form-group">
                                <label for="SightTitle">عنوان</label>
                                <p>@Model.ItemTitle</p>
                            </div>
                            <div class="form-group">
                                <label for="SightDescription">توضیح تکمیلی</label>
                                <p>@Model.ItemDescription</p>
                            </div>
                            <div class="form-group ">
                                <div class="text-center">
                                    <img id="imagePreview" class="thumbnail my-5" src="~/Images/Site/@Model.ImageName" />
                                </div>
                            </div>
                        </div>
                        <!-- /.card-body -->

                        <div class="card-footer">
                            <button type="submit" class="btn btn-danger">حذف</button>
                            <a href="/Admin/AboutUsItems" class="btn btn-info float-left">بازگشت به لیست</a>
                        </div>
                    </form>
                </div>
                <!-- /.card -->
            </div>
        </div>
    </div><!-- /.container-fluid -->
</div>
<script>
    imageProduct.onchange = evt => {
        const [file] = imageProduct.files
        if (file) {
            imagePreview.src = URL.createObjectURL(file)
        }
    }
</script>