@model DataLayer.SocialMedium
<div class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card card-warning">
                    <div class="card-header">
                        <h3 class="card-title">
                            ویرایش صفحه مجازی
                            <span class="text-dark mx-2">@ViewBag.Message</span>
                        </h3>
                    </div>
                    <!-- /.card-header -->
                    <!-- form start -->
                    <form role="form" method="post" enctype="multipart/form-data">
                        <input type="hidden" name="SocialMediaId" id="SocialMediaId" value="@Model.SocialMediaId" />
                        <input type="hidden" name="ClassName" id="ClassName" value="@Model.ClassName" />
                        <div class="card-body">
                            <div class="form-group">
                                <label for="Title">عنوان</label>
                                <input type="text" name="Title" class="form-control" value="@Model.Title" id="Link" placeholder="عنوان را وارد کنید">
                            </div>
                            <div class="form-group">
                                <label for="Link">لینک</label>
                                <input type="text" name="Link" class="form-control" value="@Model.Link" id="Link" placeholder="لینک را وارد کنید">
                            </div>
                            <div class="form-group">
                                <label for="ClassName">کلاس (کد رنگ روی کلاس تنظیم خواهد شد)</label>
                                <input type="text" name="ClassName" class="form-control" value="@Model.ClassName" id="ClassName" placeholder="کلاس را وارد کنید">
                            </div>
                            <div class="form-group">
                                <label for="ColorCode">کد رنگ</label>
                                <input type="text" name="ColorCode" class="form-control" value="@Model.ColorCode" id="ColorCode" placeholder="کد رنگ را وارد کنید">
                            </div>
                            <div class="form-group">
                                <div class="text-center" id="output">
                                    @Html.Raw(Model.ImageName)
                                </div>
                                <label for="ImageName">تصویر (1X1 - 512X512 (SVG))</label>
                                <textarea style="direction:ltr;font-family:sans-serif;" onchange="Preview()" rows="5" name="ImageName" class="form-control" id="ImageName">@Model.ImageName</textarea>
                            </div>
                            
                        </div>
                        <!-- /.card-body -->

                        <div class="card-footer">
                            <button type="submit" class="btn btn-warning">ثبت</button>
                            <a href="/Admin/SocialMedia" class="btn btn-info float-left">بازگشت به لیست</a>
                        </div>
                    </form>
                </div>
                <!-- /.card -->
            </div>
        </div>
    </div><!-- /.container-fluid -->
</div>
<script type="text/javascript">
    function Preview() {
        document.getElementById("output").innerHTML = "";
        var oInput = document.getElementById("ImageName");
        var oOutput = document.getElementById("output");
        // clean preview area
        if (oOutput.firstChild)
            oOutput.removeChild(oOutput.firstChild);
        // parse text into DOM and show it in preview
        var oSVGInput = new DOMParser().parseFromString(oInput.value, "text/xml");
        if (oSVGInput.documentElement)
            oOutput.appendChild(document.importNode(oSVGInput.documentElement, true));
        else
            oOutput.appendChild("Unknown error");
    }
</script>